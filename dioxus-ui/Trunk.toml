[[hooks]]
stage = "post_build"
command = "bash"
command_arguments = ["scripts/hash-worker-assets.sh"]

[[hooks]]
stage = "post_build"
command = "sh"
command_arguments = [
    "-c",
    "cp -f scripts/config.local.js \"${TRUNK_STAGING_DIR:-dist}/config.local.js\" 2>/dev/null || true",
]
