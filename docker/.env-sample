# Copy this file to .env in the repo root and fill in your values.
# cp docker/.env-sample .env

# *** YOU MUST MAKE CHANGES!!  Minimally the OAUTH_CLIENT_ID and OAUTH_CLIENT_SECRET ***


# OAuth — Google (OIDC discovery, recommended)
# Get credentials at: https://console.cloud.google.com/apis/credentials
# Add http://localhost:8081/login/callback as an Authorized redirect URI.
ENABLE_OAUTH=true
OAUTH_PROVIDER=google
OAUTH_CLIENT_ID=<-- your client ID here ->
OAUTH_CLIENT_SECRET=<-- your client secret here ->
OAUTH_ISSUER=https://accounts.google.com
OAUTH_REDIRECT_URL=http://localhost:8081/login/callback
AFTER_LOGIN_URL=http://localhost
ALLOWED_REDIRECT_URLS=http://localhost,http://localhost:3001

# Internal service URLs — defaults work for local Docker
ACTIX_UI_BACKEND_URL=ws://localhost:8080

# Change this to a random secret in any shared environment
JWT_SECRET=dev-jwt-secret-change-me-for-local
