{{- if and .Values.websocket.enabled .Values.websocket.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "videocall.componentFullname" (dict "root" . "component" "websocket") }}
  labels:
    {{- include "videocall.labels" (dict "root" . "component" "websocket") | nindent 4 }}
  {{- with .Values.websocket.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.websocket.ingress.className }}
  ingressClassName: {{ .Values.websocket.ingress.className }}
  {{- end }}
  {{- if .Values.websocket.ingress.tls }}
  tls:
    {{- range .Values.websocket.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.websocket.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ default (include "videocall.componentFullname" (dict "root" $ "component" "websocket")) .service.name }}
                port:
                  number: {{ .service.port.number }}
          {{- end }}
    {{- end }}
{{- end }}

