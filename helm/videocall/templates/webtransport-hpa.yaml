{{- if and .Values.webtransport.enabled .Values.webtransport.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "videocall.componentFullname" (dict "root" . "component" "webtransport") }}
  labels:
    {{- include "videocall.labels" (dict "root" . "component" "webtransport") | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "videocall.componentFullname" (dict "root" . "component" "webtransport") }}
  minReplicas: {{ .Values.webtransport.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.webtransport.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.webtransport.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.webtransport.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.webtransport.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.webtransport.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}

